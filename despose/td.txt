cd /projects/d365-test-automation && .venv/bin/python -m pytest field/tests/web/test_life_events_family_goal.py -v -s --no-header --dist=no -p no:allure_pytest --import-mode=importlib -o addopts=


env | grep -i proxy

curl -sS -o /dev/null -w "direct=%{http_code}\n" --max-time 15 "https://apps.powerapps.com/apphost/e/9b1f30fe-f32e-e94b-807e-f882add9d010/clientsdkproxy?version=1"

curl -sS -o /dev/null -w "noproxy=%{http_code}\n" --max-time 15 --noproxy '*' "https://apps.powerapps.com/apphost/e/9b1f30fe-f32e-e94b-807e-f882add9d010/clientsdkproxy?version=1"


launch(args=["--disable-quic"])

cd /projects/d365-test-automation && grep -rn "connect_over_cdp\|chromium.launch\|fresh_login" conftest.py core/ | head -20



For ticket:

getent hosts apps.powerapps.com

timeout 5 bash -c '</dev/tcp/apps.powerapps.com/443' && echo "TCP CONNECT OK" || echo "TCP CONNECT FAIL"

timeout 10 openssl s_client -connect apps.powerapps.com:443 -servername apps.powerapps.com </dev/null 2>&1 | head -8



Title: Devpod cannot reach apps.powerapps.com (443) — TLS connection reset, blocks D365 UI test automation

Environment: devpod yellow-goose-b522a-688d7c867f-fhtr2

What's wrong

From this pod, HTTPS connections to apps.powerapps.com are closed during the TLS handshake. DNS resolves and TCP connects, but the connection is dropped before the server presents its certificate. No HTTP proxy is configured in the pod, and bypassing proxy settings makes no difference — which points to hostname-based (SNI) filtering on egress rather than a proxy or DNS problem.

Why it matters

apps.powerapps.com hosts clientsdkproxy, the Dynamics 365 Client API bridge. Without it, Field CRM record forms fail to initialise, show "The script … clientsdkproxy?version=1 didn't load correctly", and navigate the user away from the record. This blocks all automated UI testing of Field CRM from the devpod.

Evidence : 

$ getent hosts apps.powerapps.com
2603:1061:14:75::1  mr-z01.tm-azurefd.net apps.powerapps.com ...

$ timeout 5 bash -c '</dev/tcp/apps.powerapps.com/443'
TCP CONNECT OK

$ openssl s_client -connect apps.powerapps.com:443 -servername apps.powerapps.com
Connecting to 150.171.110.195
CONNECTED(00000003)
error:0A000126:SSL routines::unexpected eof while reading
no peer certificate available
Negotiated TLS1.3 group: <NULL>

$ curl https://apps.powerapps.com/apphost/e/.../clientsdkproxy?version=1
curl: (35) OpenSSL SSL_connect: SSL_ERROR_SYSCALL in connection to apps.powerapps.com:443

$ env | grep -i proxy
(no output)


Already ruled out

The same tests pass when run against a browser on a corporate laptop, so the application, the URL and the credentials are all fine — the only difference is pod egress. fieldcrm-uat2.crm.dynamics.com is reachable from the pod without issue, so this is host-specific, not a general internet block.

Request

Allow outbound HTTPS (443) to apps.powerapps.com from this devpod.

Secondary — these also fail and cause console noise and missing icons, but do not block testing: *.cdn.office.net, static2.sharepointonline.com, config.edge.skype.com, fpc.msedge.net, *.sales.ai.dynamics.com.


Two things they'll likely ask, so you have the answers ready:

"Is it DNS?" No — getent resolves it, and openssl reached the IP and completed a TCP connection.

"Have you set the proxy?" There is no proxy in the pod to set; env | grep -i proxy is empty and curl --noproxy '*' behaves identically.



-----------


cd /projects/d365-test-automation && .venv/bin/python -m pytest field/tests/web/test_life_events_family_goal.py -v -s --no-header --dist=no -p no:allure_pytest --import-mode=importlib -o addopts= 2>&1 | tee /tmp/test3-net.log
cd /projects/d365-test-automation && .venv/bin/python -m pytest field/tests/web/test_life_events_family_goal.py -v -s --no-header --dist=no -p no:allure_pytest --import-mode=importlib -o addopts=

grep -nE "powerapps|clientsdk" /tmp/test3-net.log

NETDEBUG_TRACE=1 .venv/bin/python -m pytest field/tests/web/test_life_events_family_goal.py -v -s --no-header --dist=no -p no:allure_pytest --import-mode=importlib -o addopts=



I’m sorry to bring this to you, but I feel this has now gone beyond a difference of opinion on process. I am increasingly concerned about the way disagreements are being handled and the repeated friction it is creating within the team.
I have tried to ignore several instances and work around them, but the pattern is continuing. Issues are repeatedly raised in a confrontational manner, discussions that should be relatively straightforward become unnecessarily difficult, and in some cases suggestions are raised but there is resistance when it comes to following through on the agreed approach.
I want to be clear that I am not trying to assign tasks to anyone or micromanage the team. Tasks are being picked independently. My expectation is simply that once we agree on a process or responsibility, we work within it and raise disagreements constructively.
At this point, I am finding the working relationship increasingly difficult and I don’t think repeatedly handling this kind of friction between ourselves is productive. I would appreciate your intervention and some clarity around ownership and responsibilities. I am not asking for complete control of automation, and everyone should continue to contribute, but someone needs to be able to drive it with a reasonable level of cooperation from the team.
If this way of working continues, I would also prefer that our responsibilities are structured in a way that reduces the dependency on us having to work closely together.

