JSON.stringify((()=>{const N=v=>String(v||'').replace(/\u00a0/g,' ').replace(/\s+/g,' ').trim().toLowerCase();const C=v=>N(v).replace(/[.!:;…]+$/,'').replace(/\s*\((?=[^)]*(?:ctrl|alt|shift|cmd))[^)]*\)\s*$/i,'').trim();const T='task';const Q='button,a,input,textarea,select,label,li,[role],[aria-label],[data-id],[data-testid],[data-automation-id],[title],[placeholder]';const A=['data-testid','data-automation-id','data-id','aria-label','name','placeholder','title'];const R=e=>e.closest('[role="toolbar"],[role="menubar"]')?'command':e.closest('[role="menu"],[role="listbox"],[role="dialog"]')?'overlay':e.closest('[role="navigation"],[role="tree"]')?'navigation':e.closest('[role="grid"],[role="treegrid"],table')?'grid':e.closest('form,[role="form"]')?'form':'content';const out=[];for(const el of document.querySelectorAll(Q)){const vs=A.map(a=>el.getAttribute(a)).filter(Boolean);const ha=vs.some(v=>N(v)===T)||vs.some(v=>C(v)===T);const ht=N(el.textContent)===T||C(el.textContent)===T;if(!ha&&!ht)continue;const r=el.getBoundingClientRect();const an=[];let p=el.parentElement;for(let i=0;i<5&&p;i++,p=p.parentElement)an.push(p.tagName+(p.getAttribute('role')?'['+p.getAttribute('role')+']':''));out.push({tag:el.tagName,role:el.getAttribute('role'),aria:el.getAttribute('aria-label'),dataId:el.getAttribute('data-id'),title:el.getAttribute('title'),src:ha?'attr':'text',vis:r.width>0&&r.height>0,region:R(el),anc:an});}return{count:out.length,landmarks:{toolbar:document.querySelectorAll('[role="toolbar"]').length,menubar:document.querySelectorAll('[role="menubar"]').length,grid:document.querySelectorAll('[role="grid"],[role="treegrid"]').length,nav:document.querySelectorAll('[role="navigation"],[role="tree"]').length,menu:document.querySelectorAll('[role="menu"],[role="listbox"]').length},matches:out.slice(0,40)};})())



JSON.stringify([...document.querySelectorAll('[role="menuitem"],[role="option"],li,button,a')].map(e=>[e.getAttribute('aria-label'),e.getAttribute('title'),(e.textContent||'').trim(),e.tagName,e.getAttribute('role')]).filter(v=>[v[0],v[1],v[2]].some(x=>x&&/create task/i.test(x))).map(v=>({tag:v[3],role:v[4],aria:v[0],title:v[1],text:v[2],lens:[v[0],v[1],v[2]].map(x=>x?x.length:null)})))

JSON.stringify((()=>{const TERMS=["Quick Actions","Interactions","Save & Close","Contact Details","Up next","Life Goals and Milestones","Create a timeline record"];const N=v=>String(v||'').replace(/\u00a0/g,' ').replace(/\s+/g,' ').trim().toLowerCase();const C=v=>N(v).replace(/[.!:;…]+$/,'').replace(/\s*\((?=[^)]*(?:ctrl|alt|shift|cmd))[^)]*\)\s*$/i,'').trim();const Q='button,a,input,textarea,select,label,li,[role],[aria-label],[data-id],[data-testid],[data-automation-id],[title],[placeholder]';const A=['data-testid','data-automation-id','data-id','aria-label','name','placeholder','title'];const els=[...document.querySelectorAll(Q)];return TERMS.map(t=>{const n=N(t);let ea=0,et=0,da=0,dt=0,sub=0;for(const el of els){const vs=A.map(a=>el.getAttribute(a)).filter(Boolean);const tx=el.textContent||'';if(vs.some(v=>N(v)===n))ea++;else if(vs.some(v=>C(v)===n))da++;if(N(tx)===n)et++;else if(C(tx)===n)dt++;if(!vs.some(v=>N(v)===n)&&N(tx)!==n&&(vs.some(v=>N(v).includes(n))||N(tx).slice(0,250).includes(n)))sub++;}return{term:t,exactAttr:ea,exactText:et,decAttr:da,decText:dt,substringOnly:sub,wouldResolve:(ea+et+da+dt)>0};});})())
